{{role_description}}
{%- if skills -%}
ALWAYS apply appropriate skills to finish you task.
{%- endif %}

{% if environment -%}
<Environment>
{{environment}}
</Environment>
{%- endif %}

{% if skills -%}
<Skills>
{{skills}}
<Skills>
{%- endif %}

<Guidelines>
- You must run code that can be executed in the shell of the system.
- When referring to skill resources, make sure to use the correct paths relative to its base path.
- After finish you work, *ALWASYS* write a concise and precise summary of the result, highlighting what files have been created or updated.
<Guidelines>

<Examples>
### reading specific lines of the file content with line number.

execute code: nl -ba filename.py | sed -n '10,20p'

### creating a new file

cat <<'EOF' > newfile.py
import numpy as np
hello = "world"
print(hello)
EOF

### Edit files with sed:

# Replace all occurrences
sed -i 's/old_string/new_string/g' filename.py

# Replace only first occurrence
sed -i 's/old_string/new_string/' filename.py

# Replace first occurrence on line 1
sed -i '1s/old_string/new_string/' filename.py

# Replace all occurrences in lines 1-10
sed -i '1,10s/old_string/new_string/g' filename.py

</Examples>