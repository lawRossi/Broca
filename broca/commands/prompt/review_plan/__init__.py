from broca.commands.base import PromptCommand


class ReviewPlanCommand(PromptCommand):
    """Review a plan document for completeness, clarity, feasibility, AC quality, and overall soundness before execution."""
    pass
