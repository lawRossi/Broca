---
name: execute-plan
description: execute a plan with builtin skills.
type: prompt
use_sub_agent: false
argument_hint: "<你的计划文件>"
---
# 执行计划

## 任务

你的任务是执行用户指定的计划。用户输入是：{{ args }}。

## 核心原则

**对完成质量负责**

- 不能有遗漏的工作项，不能标记TODO而不实现
- 实现不能偷工减料，必须严格按照计划的方案执行
- 诚实透明，如果有不能实现的阻碍项，必须向用户报告，不能敷衍处理
- 报告完成情况前必须详细检查和测试

## 核心方法论

**采用持续集成、持续交付的工作方式**

每次只完成计划的一个阶段，拆解任务，完成任务，完成阶段结果检查，最后完成与已有结果的集成和检查。

## 任务步骤

### 1. 深入理解与评估

1. 仔细阅读计划文件，充分理解计划内容，确保完全掌握需求与目标
- 必须充分理解计划的背景，尝试从计内容中找到参考资料，并熟悉参考资料的内容
- 如果涉及workplace内容探索，可借助explorer; 如果涉及外部信息查询，可使用web_search、web_fetch工具

2. 对计划进行批判性评估
- 可行性评估：评估计划目标是否合理，方案是否可行。如果整体不可行，则停止执行该计划，并说明理由。
- 完整性评估：评估方案是否内容完整。如果存在重要内容缺失，必须反馈给用户。
- 清晰度评估：评估方案是否内容清晰无歧义。如果存在需要澄清的问题或决策点，必须在实现前用户确认清楚，可以给出建设性的建议。

### 2. 按阶段完成计划执行

采用持续集成、持续交付的工作方式，每次只执行一个阶段的计划。

1. 任务拆解
使用plan-with-tasks技能，将当前阶段计划转换成任务。每个任务的内容和接受标准必须写清楚，尤其是接受标准必须具体可衡量。

2. 任务执行
使用execute-tasks技能，按顺序逐个执行任务。

3. 结果自查
完成该阶段所有任务后，必须对该阶段的结果进行阶段自查。如果是开发任务，必须有单元测试和集成测试，并且保证测试全部通过。

4. 结果集成与自查
将该阶段的结果与已有结果集成，完成集成结果自查。

5. 编写阶段报告
每个阶段完成后必须编写阶段报告，报告文件路径：plans/reports/execution/{plan-name}-{stage}.md。
报告内容严格按照下面的报告模板编写。

6. 提交审核
阶段报告完成后，只需简单说明阶段执行已完成，提供阶段报告文件路径，等待审核结果。

7. 审核结果
- 如果审核结果通过，则开始下一阶段的执行。
- 如果审核结果有问题，则针对报告的每个问题进行修复：更新任务（状态、描述、接受条件），重新执行任务，完成后更新报告，最终更新阶段报告再次提交审核。

**必须保证结果的质量，没有经过细致检查，不要报告任务已完成**

## 阶段报告模版

```markdown
# {计划名称} — {阶段名称} — 执行报告

## 执行概览

| 项目 | 内容 |
|------|------|
| 计划文件 | {计划文件路径} |
| 阶段 | Phase {N}: {阶段名称} |
| 状态 | ✅ 通过 / ⚠️ 部分通过 / ❌ 未通过 |

## 任务完成情况

| 任务 | 状态 | 接受标准 | 实际结果 | 备注 |
|:----:|:----:|----------|----------|------|
| Task {N}.{M}: {任务名称} | ✅ / ⚠️ / ❌ | {接受标准描述} | {实际完成情况} | {如有问题或偏差} |
| Task {N}.{M+1}: {任务名称} | ✅ / ⚠️ / ❌ | {接受标准描述} | {实际完成情况} | {如有问题或偏差} |
| ... | ... | ... | ... | ... |

> 状态说明：✅ = 完成且达标，⚠️ = 完成但部分未达标，❌ = 未完成

## 测试结果

### 单元测试结果

### 集成测试结果


## 集成检查

| 检查项 | 结果 | 说明 |
|--------|:----:|------|
| 代码编译/语法检查 | ✅/❌ | {详细说明} |
| 与已有代码集成 | ✅/❌ | {详细说明} |
| 接口兼容性 | ✅/❌ | {详细说明} |
| 数据一致性 | ✅/❌ | {详细说明} |

## 质量检查清单

- [ ] 所有预期产出已创建
- [ ] 所有验收测试通过
- [ ] 代码符合项目规范（命名、风格、类型提示等）
- [ ] 错误处理和边界情况已覆盖
- [ ] 文档（注释、README等）已更新
- [ ] 无硬编码、无安全隐患
- [ ] 与现有功能兼容

## 偏差说明

{如果执行与计划存在偏差，在此说明偏差内容、原因和影响}
